use("commerce");
db.produtos.find(
  {nome: {$regex: /Mc/i}}
);

db.produtos.find(
  {},
  { nome: 1, tags: 1, _id: 0 },  
);

use("commerce");db.produtos.find();

db.products.find({ sku: { $regex: /^ABC/i } });
