use("commerce");
db.produtos.createIndex({ descricao: "text" },{ default_language: "portuguese" });
db.produtos.find(
  {
    $text: { 
       $search: "frango hamburguer",
      },
    
  },
).count();

db.produtos.find(
  {},
  { nome: 1, tags: 1, _id: 0 },  
);

use("commerce");db.produtos.find();

db.products.find({ sku: { $regex: /^ABC/i } });
